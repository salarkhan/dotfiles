windows:
  - name: vi
    root: ~/sosh
    panes:
      - vi .
      focus: true
  - name: bash
    root: ~/sosh
    layout: even-horizontal
    panes:
      - git status
  - name: logs
    root: ~/logs
    panes:
      - tail -f offline-web-stdout.log
